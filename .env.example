# SnapProof .env example
# Copy this to `.env` in the project root and fill the values.

# Toggle Google Sheets logging (True/1/yes to enable)
USE_SHEETS=True

# Path to your service account JSON file (e.g. credentials.json)
GOOGLE_CREDENTIALS=credentials.json

# The spreadsheet ID from the Google Sheets URL
# Example URL: https://docs.google.com/spreadsheets/d/1AbCDeFGhiJklMN-opQRSTuvWXyz/edit
# The SPREADSHEET_ID is the long ID between /d/ and /edit
GOOGLE_SHEETS_ID=your_spreadsheet_id_here
